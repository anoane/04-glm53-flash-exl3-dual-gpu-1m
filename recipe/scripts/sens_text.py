TEXT = [
    "def merge_sorted(a, b):\n    i = j = 0\n    out = []\n    while i < len(a) and j < len(b):\n",
    "class RedBlackTree:\n    def __init__(self):\n        self.nil = Node(color=BLACK)\n",
    "import asyncio\n\nasync def fetch_all(urls, limit=8):\n    sem = asyncio.Semaphore(limit)\n",
    "template <typename T>\nclass RingBuffer {\npublic:\n    explicit RingBuffer(size_t cap)\n",
    "fn parse_header(buf: &[u8]) -> Result<Header, ParseError> {\n    if buf.len() < 8 {\n",
    "SELECT u.id, COUNT(o.id) AS n\nFROM users u\nLEFT JOIN orders o ON o.user_id = u.id\n",
    "#include <stdint.h>\nstatic inline uint32_t rotl32(uint32_t x, int r) {\n    return (x << r)\n",
    "The transistor was invented in 1947 at Bell Labs. Its significance lies in replacing\n",
    "In a sparse mixture-of-experts model, each token is routed to a small subset of experts,\n",
    "func (s *Server) handleRequest(w http.ResponseWriter, r *http.Request) {\n    ctx := r.Context()\n",
]
